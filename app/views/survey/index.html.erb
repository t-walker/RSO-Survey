<h1>The Survey</h1>

<% @questionList.each_with_index do |q, qestindex| %>
  <%= render partial: 'question', :locals => {:qid => q.id, :questionTitle => q.question, :qindex => qestindex, :answers => q.answers} %>
<% end %>

<a href="/">Back to the homepage</a>
