<div>
  <h4>
    <%= questionTitle %>
  </h4>
  <ul>
    <% answers.each_with_index do |a, aindex| %>
      <%= render partial: 'question-answer', :locals => {:questId => qid, :answerTitle => a.answer, :answerIndex => aindex} %>
    <% end %>
  </ul>
  <hr />

</div>
